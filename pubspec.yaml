name: eq_barcode_field
description: "A new Flutter package project."
version: 0.0.1
homepage:

environment:
  sdk: ^3.5.3
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  barcode_newland_flutter: ^1.0.1
  qr_code_scanner_plus: ^2.0.6
  

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

 
flutter:
 